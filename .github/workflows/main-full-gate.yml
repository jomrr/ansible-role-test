---
# role: test
# file: .github/workflows/main-full-gate.yml

name: main full gate

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  molecule-full:
    name: Molecule full ${{ matrix.name }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45

    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        include:
          - name: test-almalinux-latest
            uns: jomrr
            img: almalinux
            tag: latest
          - name: test-alpine-latest
            uns: jomrr
            img: alpine
            tag: latest
          - name: test-debian-latest
            uns: jomrr
            img: debian
            tag: latest
          - name: test-fedora-latest
            uns: jomrr
            img: fedora
            tag: latest
          - name: test-opensuse-leap-latest
            uns: jomrr
            img: opensuse-leap
            tag: latest
          - name: test-opensuse-tumbleweed-latest
            uns: jomrr
            img: opensuse-tumbleweed
            tag: latest
          - name: test-ubuntu-latest
            uns: jomrr
            img: ubuntu
            tag: latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Show Podman environment
        run: |
          set -euo pipefail
          podman --version
          podman info

      - name: Run Molecule default scenario
        run: |
          set -euo pipefail
          export uns="${{ matrix.uns }}"
          export img="${{ matrix.img }}"
          export tag="${{ matrix.tag }}"
          uvx \
            --from molecule \
            --with ansible-core \
            --with ansible-lint \
            --with "molecule-plugins[podman]" \
            molecule test \
            -s "default"
