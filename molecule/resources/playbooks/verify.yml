---
# role: test
# file: molecule/resources/playbooks/verify.yml

- name: TEST | Verify CI demonstration
  hosts: all
  gather_facts: false
  tasks:
    - name: TEST | Report completion of the no-op scenario
      ansible.builtin.debug:
        msg: "CI demonstration completed; this role manages no application."
